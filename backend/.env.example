# ============================================================
# VENTURI — Backend Environment Variables
# ============================================================
# On Render / Hosting platform: add these in Environment Variables settings.
# Locally: defaults are used automatically (no .env needed).

# Allowed frontend origins for CORS (comma-separated if multiple):
FRONTEND_URL=http://localhost:3000,https://your-frontend.vercel.app
